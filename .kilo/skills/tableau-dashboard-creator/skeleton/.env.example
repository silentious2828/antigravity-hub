# =============================================================================
# Database Credentials — fill in the section matching your database type.
# Rename this file to .env before use.
# =============================================================================

# --- PostgreSQL ---
# PG_HOST=localhost
# PG_PORT=5432
# PG_DATABASE=your_database
# PG_USER=your_user
# PG_PASSWORD=your_password

# --- Add your own database credentials below ---
# If you added a custom query script (e.g., query_databricks.py, query_snowflake.py),
# add the required env vars here following the same pattern.
